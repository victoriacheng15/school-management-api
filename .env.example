# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# 
# This file contains both local and Azure database configurations.
# The application automatically selects the appropriate database based on FLASK_ENV:
#
# FLASK_ENV=development  -> Uses LOCAL_DB_* variables (Docker Compose)
# FLASK_ENV=production   -> Uses AZURE_DB_* variables (Azure Database)
#
# To switch environments, simply change the FLASK_ENV value below.
# =============================================================================

# FLASK_ENV=production
FLASK_ENV=development
SECRET_KEY=some-long-random-secret

# Local Development Database (Docker Compose)
LOCAL_DB_HOST=
LOCAL_DB_PORT=
LOCAL_DB_NAME=
LOCAL_DB_USER=
LOCAL_DB_PASSWORD=

# Azure Production Database
AZURE_PG_HOST=
AZURE_PG_PORT=
AZURE_PG_NAME=
AZURE_PG_USER=
AZURE_PG_PASSWORD=
AZURE_PG_SSLMODE=